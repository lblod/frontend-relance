<AuLabel>{{@label}}</AuLabel>
<AuInput
  @value={{this._value}}
  @error={{this.hasError}}
  {{on "blur" this.updateValue}}
/>
{{#if this.error}}
  <AuHelpText @error={{"error"}}>Ongeldig formaat. Gebruik HH:mm.</AuHelpText>
{{/if}}
