<AnimatedContainer>
  <AnimatedIf @predicate={{this.predicate}} @use={{this.transition}} @initialInsertion={{this.appState.didRender}} @finalRemoval={{true}} @duration={{200}}>
    {{yield}}
  </AnimatedIf>
</AnimatedContainer>
