<AnimatedContainer>
  <AnimatedIf @predicate={{this.predicate}} @use={{this.transition}} @initialInsertion={{true}} @finalRemoval={{true}} @duration={{200}}>
    {{yield}}
  </AnimatedIf>
</AnimatedContainer>
