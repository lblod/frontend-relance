import pageTransition from 'frontend-relance/utils/page-transition';
import { module, test } from 'qunit';

module('Unit | Utility | page-transition', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pageTransition();
    assert.ok(result);
  });
});
